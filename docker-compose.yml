version: '3.8'

services:
  streams-game:
    build: .
    container_name: streams-boardgame
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    networks:
      - streams-network

networks:
  streams-network:
    driver: bridge
